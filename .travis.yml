language: c

sudo: false

env:
    - DOTFILES_ROOT="$(readlink -m ..)"

before_script:
    - git clone https://github.com/sstephenson/bats.git

script:
    - ./bats/bin/bats --tap ./test

notifications:
    email:
        on_success: never
