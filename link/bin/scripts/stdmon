#!/bin/bash
IN="DVI-D-0"
IN_MODE="1920x1080"
EXT="DVI-I-0"

if [[ -n `xrandr | grep "$EXT disconnected"` ]]; then
    xrandr --output $IN --auto --primary --output $EXT --off
else
    xrandr --output $IN --auto --primary --output $EXT --auto --rotate left --right-of $IN
fi


